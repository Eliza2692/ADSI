@model IEnumerable<ICBF.Models.asignacionjardin>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_asig)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Datninos.nomape)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Regjardines.ced)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_asig)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Datninos.nomape)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Regjardines.ced)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
