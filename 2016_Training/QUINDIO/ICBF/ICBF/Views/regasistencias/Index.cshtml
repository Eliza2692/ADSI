@model IEnumerable<ICBF.Models.regasistencia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMadres.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estado_nino)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Datninos.nomape)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estado_nino)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Datninos.nomape)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        </td>
    </tr>
}

</table>
